<% include partials/header %>
<h1>Campgrounds Page</h1>

<% campgrounds.forEach(function(campground){ %>
    <div>
        <h4><%= campground.name %></h4>
        <img src='<%= campground.image %>' style='height: 400px' />
    </div>
<% }); %>

<a href="/">Go Home</a>

<% include partials/footer %>